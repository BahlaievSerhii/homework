import { exportAllDeclaration } from '@babel/types'
import { functions } from './date.js'

describe("date.test", () => {
    test ("",() => {
        const now = function compareDate();
        expect (now).toBe(false);
    })
})